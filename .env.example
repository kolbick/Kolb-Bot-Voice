# Relay WebSocket URL — get this from the cloudflared tunnel running on your server
# Replace with the current tunnel URL each time cloudflared restarts (or use a named tunnel for a fixed URL)
RELAY_URL=wss://your-tunnel-url.trycloudflare.com/relay

# Your ElevenLabs agent ID
AGENT_ID=agent_3201kj2x8772fyxv3et1cptrbyed
